[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = ["npx prisma generate --schema=./prisma/schema.prisma"]

[start]
cmd = "node server.js"
